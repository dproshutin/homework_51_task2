import React from 'react';

const Promo = (props) => {
    return (
        <div className="promo">
            <img src={props.image} alt="Slider picture"/>
        </div>
    );
};

export default Promo;